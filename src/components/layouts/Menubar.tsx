import * as React from "react";

const asideStyle = {
  backgroundColor: "#272822",
  minHeight: "100vh",
  minWidth: "48px",
};

export default function Menubar() {
  return <aside style={asideStyle}>asdf</aside>;
}
